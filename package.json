{
  "name": "c3-blog",
  "version": "1.0.0",
  "description": "Cards on Cards on Cards - Eleventy blog",
  "scripts": {
    "start": "eleventy --serve",
    "build": "node scripts/generate-sitemap-cards.mjs && eleventy",
    "build:fast": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.1",
    "stream-json": "^1.7.3"
  }
}
